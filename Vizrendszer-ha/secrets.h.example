// ────────────────────────────────────────────────────
// Vizrendszer-ha — Secrets (NOT TRACKED IN GIT)
//
// Copy this file to secrets.h and fill in your values.
// ────────────────────────────────────────────────────

#pragma once
#include <IPAddress.h>

// MQTT Broker (your Home Assistant machine running Mosquitto)
inline const IPAddress MQTT_BROKER_IP(192, 168, 1, 100);
inline const uint16_t  MQTT_BROKER_PORT = 1883;

// MQTT credentials — leave as nullptr for anonymous access
// If your broker requires authentication, replace with actual strings:
//   inline const char* MQTT_USER     = "mqtt_user";
//   inline const char* MQTT_PASSWORD = "mqtt_pass";
inline const char* MQTT_USER     = nullptr;
inline const char* MQTT_PASSWORD = nullptr;
